{
  "name": "epg-tempest-integrated",
  "version": "1.0.0",
  "main": "index.js",
  "workspaces": [
    "epg-grabber"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "grab": "cd epg-grabber && npm run grab -- --site=foxtel.com.au",
    "merge": "node index.js",
    "grab-n-merge": "npm run grab && npm run merge"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "xml2js": "^0.6.2"
  }
}
